import spaces from './spaces'
import colors from './colors'
import {
  fonts, fontSizes, fontWeights, lineHeights, text,
} from './typography'


export default {
  spaces,
  colors,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  text,
}
